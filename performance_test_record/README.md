# performance_test_record

This directory stores reusable RMDB performance workflow outputs.

Each run should create a timestamped subdirectory, for example:

```text
performance_test_record/20260613_151500_sf1_t4/
```

Keep:

- `README.md`
- `.gitignore`

Generated run artifacts should remain untracked by default.
